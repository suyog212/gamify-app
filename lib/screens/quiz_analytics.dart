import 'package:flutter/material.dart';

class QuizAnalytics extends StatelessWidget {
  const QuizAnalytics({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: const Text("Quiz Stats"),
      ),
    );
  }
}